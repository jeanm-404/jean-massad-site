{
  "name": "jean-massad-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio — no framework, precompiled JSX for production.",
  "scripts": {
    "build": "babel app.jsx -o app.compiled.js --presets @babel/preset-react",
    "deploy": "npm run build && vercel deploy --prod --yes"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-react": "^7.24.0"
  }
}
